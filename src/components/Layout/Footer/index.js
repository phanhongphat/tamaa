import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
	return <div />;
};

Footer.propTypes = {};

Footer.defaultProps = {
	// classes: {},
};
export default Footer;
